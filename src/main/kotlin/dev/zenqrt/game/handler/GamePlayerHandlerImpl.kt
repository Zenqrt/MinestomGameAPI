package dev.zenqrt.game.handler

import dev.zenqrt.game.GamePlayer

internal class GamePlayerHandlerImpl : GamePlayerHandler {

    override fun join(gamePlayer: GamePlayer) {
    }

    override fun leave(gamePlayer: GamePlayer) {
    }
}