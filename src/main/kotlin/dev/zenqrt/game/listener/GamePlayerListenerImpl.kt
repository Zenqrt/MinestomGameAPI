package dev.zenqrt.game.listener

import dev.zenqrt.game.GamePlayer

class GamePlayerListenerImpl : GamePlayerListener {

    override fun onJoin(gamePlayer: GamePlayer) {
    }

    override fun onLeave(gamePlayer: GamePlayer) {
    }
}